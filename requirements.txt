pip==24.0  # Avoid the latest pip version if it's causing conflicts
setuptools>=68.0.0
wheel
streamlit==1.31.0
Pillow==10.0.0
pypdfium2==4.20.0
pandas==2.1.4
numpy==1.26.3
scipy
moviepy==1.0.3
openai==0.28.1
gTTS==2.4.0
python-pptx==0.6.21
python-magic
rich==13.9.4  # Explicitly define rich version
pygments==2.19.1  # Explicitly define pygments version
markdown-it-py==3.0.0  # Avoid auto reinstallation issues
mdurl==0.1.2  # Avoid auto reinstallation issues
